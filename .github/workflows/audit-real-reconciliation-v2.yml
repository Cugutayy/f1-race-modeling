name: Resilient real provider reconciliation audit

on:
  push:
    branches:
      - audit/real-provider-reconciliation-v2

permissions:
  contents: read

jobs:
  audit-australia-2025:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: requirements-lock.txt
      - run: python -m pip install -r requirements-lock.txt
      - run: python -m pip install -e . --no-deps
      - name: Audit 2025 Australian Grand Prix
        continue-on-error: true
        run: |
          python -m f1_research.provider_audit \
            --year 2025 \
            --round 1 \
            --openf1-session-key 9693 \
            --cache data/reconciliation-cache \
            --output reports/reconciliation/2025-R1
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: resilient-provider-reconciliation-2025-R1
          path: reports/reconciliation
          if-no-files-found: error
          retention-days: 30
